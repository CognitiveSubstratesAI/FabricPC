var documenterSearchIndex = {"docs":
[{"category":"section","location":"api/#API-Reference","page":"API","text":"Auto-generated from inline docstrings, grouped to match src/FabricPC.jl's own export sections.","title":"API Reference"},{"category":"section","location":"api/#Activations","page":"API","text":"","title":"Activations"},{"category":"section","location":"api/#Energy-functionals","page":"API","text":"","title":"Energy functionals"},{"category":"section","location":"api/#Initializers","page":"API","text":"","title":"Initializers"},{"category":"section","location":"api/#Topology","page":"API","text":"","title":"Topology"},{"category":"section","location":"api/#Nodes","page":"API","text":"","title":"Nodes"},{"category":"section","location":"api/#Phase-D-autodiff-seam","page":"API","text":"A node implementing only compute_mu gets its local PC gradients for free via reverse-mode AD (Zygote or Enzyme — exactly one backend per session, see decisions.md §19/§23).","title":"Phase-D autodiff seam"},{"category":"section","location":"api/#Transformer-nodes","page":"API","text":"","title":"Transformer nodes"},{"category":"section","location":"api/#Hopfield","page":"API","text":"","title":"Hopfield"},{"category":"section","location":"api/#Core-types","page":"API","text":"","title":"Core types"},{"category":"section","location":"api/#muPC-scaling","page":"API","text":"","title":"muPC scaling"},{"category":"section","location":"api/#Inference","page":"API","text":"","title":"Inference"},{"category":"section","location":"api/#Graph-assembly-initialization","page":"API","text":"","title":"Graph assembly + initialization"},{"category":"section","location":"api/#Training","page":"API","text":"","title":"Training"},{"category":"section","location":"api/#Autoregressive-/-sequence-training","page":"API","text":"","title":"Autoregressive / sequence training"},{"category":"section","location":"api/#JIT-(Dict-free-flat-lane-—-foundation-for-Reactant)","page":"API","text":"","title":"JIT (Dict-free flat lane — foundation for Reactant)"},{"category":"type","location":"api/#FabricPC.AbstractActivation","page":"API","text":"AbstractActivation\n\nElement-wise (or, for SoftmaxActivation, near-element-wise) nonlinearity. Every concrete subtype implements forward/derivative via dispatch — there is no generic fallback. variance_gain/jacobian_gain default to 1.0 (exact for Identity/ReLU/LeakyReLU) and are overridden by activations that contract variance.\n\n\n\n\n\n","title":"FabricPC.AbstractActivation"},{"category":"type","location":"api/#FabricPC.IdentityActivation","page":"API","text":"IdentityActivation()\n\nIdentity activation: f(x) = x, f'(x) = 1.\n\n\n\n\n\n","title":"FabricPC.IdentityActivation"},{"category":"type","location":"api/#FabricPC.SigmoidActivation","page":"API","text":"Sigmoid: σ(x) = 1/(1+e⁻ˣ); σ'(x) = σ(1-σ).\n\n\n\n\n\n","title":"FabricPC.SigmoidActivation"},{"category":"type","location":"api/#FabricPC.TanhActivation","page":"API","text":"Tanh: f(x) = tanh(x); f'(x) = 1 - tanh²(x).\n\n\n\n\n\n","title":"FabricPC.TanhActivation"},{"category":"type","location":"api/#FabricPC.ReLUActivation","page":"API","text":"ReLU: f(x) = max(0, x); f'(x) = 1[x>0].\n\n\n\n\n\n","title":"FabricPC.ReLUActivation"},{"category":"type","location":"api/#FabricPC.LeakyReLUActivation","page":"API","text":"Leaky ReLU: f(x) = x>0 ? x : αx; f'(x) = x>0 ? 1 : α.\n\n\n\n\n\n","title":"FabricPC.LeakyReLUActivation"},{"category":"type","location":"api/#FabricPC.SoftplusActivation","page":"API","text":"Softplus: f(x) = log(1 + eˣ) — a smooth, strictly-positive (≥0) activation; f'(x) = σ(x) = 1/(1 + e⁻ˣ). Numerically stable via log1p(exp(-|x|)) + max(x, 0). Use for outputs that must stay positive — e.g. the SubRep MDN support head's cone bounds (examples/subrep_mdn.jl), matching the iCog generator/mdn.py softplus head (strict positivity, unlike ReLU which can pin a cone bound to exactly 0).\n\n\n\n\n\n","title":"FabricPC.SoftplusActivation"},{"category":"type","location":"api/#FabricPC.GeluActivation","page":"API","text":"GELU (tanh approximation): f(x) = 0.5·x·(1 + tanh(√(2/π)(x + 0.044715x³))). Matches upstream exactly: jax.nn.gelu's default is approximate=True (the tanh form), and derivative is hand-derived for the same approximation — conformance-verified bit-for-bit against upstream, rtol 1e-6 (test/conformance/testtiera.jl). An earlier version of this comment claimed upstream's forward used erf-GELU by default; it does not (inspect.signature(jax.nn.gelu) confirms approximate: bool = True) — there was never an inconsistency to resolve here.\n\n\n\n\n\n","title":"FabricPC.GeluActivation"},{"category":"type","location":"api/#FabricPC.HardTanhActivation","page":"API","text":"Hard tanh: f(x) = clamp(x, lo, hi); f'(x) = 1[lo<x<hi].\n\n\n\n\n\n","title":"FabricPC.HardTanhActivation"},{"category":"type","location":"api/#FabricPC.SoftmaxActivation","page":"API","text":"Softmax over the last axis: f(x)_i = e^{x_i} / Σ_j e^{x_j}.\n\nNON-element-wise. derivative returns the DIAGONAL of the Jacobian, s·(1-s) — upstream's sanctioned approximation \"valid for element-wise PC gradients\" (the off-diagonal -s_i s_j terms are dropped). Pair with CrossEntropyEnergy for classification. The explicit gradient is therefore approximate for Softmax (unlike the exact element-wise activations); see docs/decisions.md.\n\n\n\n\n\n","title":"FabricPC.SoftmaxActivation"},{"category":"function","location":"api/#FabricPC.variance_gain","page":"API","text":"variance_gain(a::AbstractActivation) -> Float32\n\nmuPC forward-scaling gain: the Kaiming-style g such that pre-activation Var(z) = g² gives post-activation Var(f(z)) ≈ 1. Used in a = gain / sqrt(fan_in * K). Defaults to 1.0 (exact for Identity/ReLU/LeakyReLU); nonlinear activations override with a precomputed constant.\n\n\n\n\n\n","title":"FabricPC.variance_gain"},{"category":"function","location":"api/#FabricPC.jacobian_gain","page":"API","text":"jacobian_gain(a::AbstractActivation) -> Float32\n\nmuPC top-down-gradient compensation factor: 1 / (variance_gain(a) · rms(a'(z))) for z ~ N(0, variance_gain(a)^2), so topdown_grad_scale = a · jacobian_gain(a) yields ~1.0 per-hop gradient propagation. Defaults to 1.0 (exact for Identity/ReLU/LeakyReLU); nonlinear activations override with a precomputed constant.\n\n\n\n\n\n","title":"FabricPC.jacobian_gain"},{"category":"function","location":"api/#FabricPC.jacobian","page":"API","text":"jacobian(a::AbstractActivation, x::AbstractMatrix) -> Array\n\nFull Jacobian J[b,i,j] = ∂f(x)_i/∂x_j, shape (B, D, D). For element-wise activations this is the diagonal embedding of derivative(a, x); SoftmaxActivation overrides with the true off-diagonal terms. Convenience for explicit (non-autodiff) gradient paths that need the full matrix, not just the diagonal derivative this codebase's PC gradients normally use.\n\n\n\n\n\n","title":"FabricPC.jacobian"},{"category":"type","location":"api/#FabricPC.AbstractEnergy","page":"API","text":"AbstractEnergy\n\nScores a node's local prediction error E(z_latent, z_mu). Every concrete subtype (GaussianEnergy, BernoulliEnergy, CrossEntropyEnergy, LaplacianEnergy, HuberEnergy, KLDivergenceEnergy) implements energy/grad_latent/grad_mu via dispatch — there is no generic fallback.\n\n\n\n\n\n","title":"FabricPC.AbstractEnergy"},{"category":"type","location":"api/#FabricPC.GaussianEnergy","page":"API","text":"GaussianEnergy(; precision = 1.0)\n\nGaussian (quadratic) energy: E = 0.5 * precision * Σ (z - μ)², summed over all non-batch dimensions. precision = 1/σ². This is the standard MSE-based energy and the default for every node type.\n\ngrad_latent returns precision * (z - μ).\n\nNote (faithful to upstream): the explicit per-edge input/weight gradients in Linear use gain_mod_error = error * f'(pre) WITHOUT a precision factor — they implicitly assume precision = 1 (the default). grad_latent keeps the factor. At the default precision the two are consistent; non-unit precision in the explicit path is a known upstream simplification we replicate verbatim.\n\n\n\n\n\n","title":"FabricPC.GaussianEnergy"},{"category":"type","location":"api/#FabricPC.BernoulliEnergy","page":"API","text":"BernoulliEnergy(; eps = 1e-7)\n\nBinary cross-entropy energy E = -Σ[z·log μ + (1-z)·log(1-μ)] for binary targets with μ ∈ (0,1) (pair with SigmoidActivation). Port of BernoulliEnergy.\n\n\n\n\n\n","title":"FabricPC.BernoulliEnergy"},{"category":"type","location":"api/#FabricPC.CrossEntropyEnergy","page":"API","text":"CrossEntropyEnergy(; eps = 1e-7)\n\nCategorical cross-entropy E = -Σ z_i·log μ_i for one-hot targets with μ a probability vector (pair with SoftmaxActivation). Port of CrossEntropyEnergy.\n\n\n\n\n\n","title":"FabricPC.CrossEntropyEnergy"},{"category":"type","location":"api/#FabricPC.LaplacianEnergy","page":"API","text":"LaplacianEnergy(; scale = 1.0)\n\nL1 energy E = (1/b)·Σ|z - μ| (robust to outliers). Port of LaplacianEnergy.\n\n\n\n\n\n","title":"FabricPC.LaplacianEnergy"},{"category":"type","location":"api/#FabricPC.HuberEnergy","page":"API","text":"HuberEnergy(; delta = 1.0)\n\nSmooth-L1 (Huber) energy: quadratic within |z-μ| ≤ delta, linear beyond. Port of HuberEnergy.\n\n\n\n\n\n","title":"FabricPC.HuberEnergy"},{"category":"type","location":"api/#FabricPC.KLDivergenceEnergy","page":"API","text":"KLDivergenceEnergy(; eps = 1e-7)\n\nE = D_KL(z ‖ μ) = Σ z·log(z/μ) for probability-vector z and μ. Port of KLDivergenceEnergy.\n\n\n\n\n\n","title":"FabricPC.KLDivergenceEnergy"},{"category":"function","location":"api/#FabricPC.energy","page":"API","text":"energy(e::AbstractEnergy, z_latent, z_mu) -> Vector\n\nPer-sample energy, shape (batch,). Dispatches on the concrete AbstractEnergy subtype.\n\n\n\n\n\n","title":"FabricPC.energy"},{"category":"function","location":"api/#FabricPC.grad_latent","page":"API","text":"grad_latent(e::AbstractEnergy, z_latent, z_mu)\n\n∂E/∂z_latent (the node's self-latent gradient), same shape as z_latent. Dispatches on the concrete AbstractEnergy subtype.\n\n\n\n\n\n","title":"FabricPC.grad_latent"},{"category":"function","location":"api/#FabricPC.grad_mu","page":"API","text":"grad_mu(e::AbstractEnergy, z_latent, z_mu)\n\n∂E/∂z_mu, same shape as z_mu. An addition beyond a straight port of upstream (which gets this via autodiff at the call site) — every energy here has an explicit closed form so the node-local explicit-gradient path generalizes with no autodiff. Dispatches on the concrete AbstractEnergy subtype.\n\n\n\n\n\n","title":"FabricPC.grad_mu"},{"category":"type","location":"api/#FabricPC.AbstractInitializer","page":"API","text":"AbstractInitializer\n\nContext-agnostic tensor initializer (weights or latent states — the caller picks the shape). Every concrete subtype implements initialize(rng, shape, init) via dispatch. Threads a plain AbstractRNG (not a JAX-style PRNGKey) — bit-parity with upstream's RNG stream is neither possible nor a goal; within-Julia determinism under a seeded RNG is what's tested.\n\n\n\n\n\n","title":"FabricPC.AbstractInitializer"},{"category":"type","location":"api/#FabricPC.ZerosInitializer","page":"API","text":"ZerosInitializer()\n\nInitialize with zeros. Used for biases.\n\n\n\n\n\n","title":"FabricPC.ZerosInitializer"},{"category":"type","location":"api/#FabricPC.NormalInitializer","page":"API","text":"NormalInitializer(; mean = 0.0, std = 0.05, gain = 1.0)\n\nDraw from N(mean, (gain·std)²): mean + gain·std·randn.\n\n\n\n\n\n","title":"FabricPC.NormalInitializer"},{"category":"type","location":"api/#FabricPC.MuPCInitializer","page":"API","text":"MuPCInitializer(; gain = 1.0)\n\nmuPC weight init: W ~ gain · N(0, 1) — UNIT variance. The width/depth scaling is deliberately NOT baked into the weights; it is applied in the forward pass via the per-edge muPC factors (core/mupc.jl). This decoupling of init from forward-scaling is the core of muPC (Yang et al.; Innocenti et al.). Port of MuPCInitializer.\n\nThis is the init muPC REQUIRES — pairing the muPC scaling with a small-std init (e.g. NormalInitializer(std=0.05)) double-shrinks the effective weights and breaks the parameterization.\n\n\n\n\n\n","title":"FabricPC.MuPCInitializer"},{"category":"type","location":"api/#FabricPC.XavierInitializer","page":"API","text":"XavierInitializer(; gain = 1.0, distribution = :normal)\n\nXavier/Glorot init, shape-aware over ANY rank — Linear (in, out) or an ND conv kernel (*spatial, C_in, C_out); see _fans. Port of XavierInitializer (both variants):\n\n:normal  — N(0, std²), std = gain·√(2/(fan_in+fan_out))\n:uniform — U(−limit, limit), limit = gain·√(6/(fan_in+fan_out))\n\n\n\n\n\n","title":"FabricPC.XavierInitializer"},{"category":"type","location":"api/#FabricPC.KaimingInitializer","page":"API","text":"KaimingInitializer(; mode = :fan_in, nonlinearity = :relu,\n                   distribution = :normal, a = 0.01, gain = 1.0)\n\nKaiming/He init optimized for ReLU networks (preserves activation variance). Port of KaimingInitializer. Shape-aware over ANY rank — Linear (in, out) or an ND conv kernel (*spatial, C_in, C_out), e.g. Conv2D (kH, kW, C_in, C_out) ⇒ fan_in = kH*kW*C_in (see _fans):\n\nfan = fan_in (:fan_in) or fan_out (:fan_out)\nReLU gain √2; leaky-ReLU gain √(2/(1+a²)); scaled by gain\n:normal  — std = gain·g/√fan\n:uniform — limit = gain·g·√(3/fan)\n\n\n\n\n\n","title":"FabricPC.KaimingInitializer"},{"category":"type","location":"api/#FabricPC.OnesInitializer","page":"API","text":"OnesInitializer()\n\nInitialize with ones. Port of OnesInitializer.\n\n\n\n\n\n","title":"FabricPC.OnesInitializer"},{"category":"type","location":"api/#FabricPC.UniformInitializer","page":"API","text":"UniformInitializer(; minval = -0.05, maxval = 0.05)\n\nUniform init U(minval, maxval). Port of UniformInitializer.\n\n\n\n\n\n","title":"FabricPC.UniformInitializer"},{"category":"function","location":"api/#FabricPC.initialize","page":"API","text":"initialize(rng::AbstractRNG, shape::Tuple, init::AbstractInitializer) -> Array{Float32}\n\nSample an array of the given shape under the given initializer. Dispatches on the concrete AbstractInitializer subtype (ZerosInitializer, NormalInitializer, MuPCInitializer, XavierInitializer, KaimingInitializer, OnesInitializer, UniformInitializer).\n\n\n\n\n\n","title":"FabricPC.initialize"},{"category":"type","location":"api/#FabricPC.SlotSpec","page":"API","text":"SlotSpec(name, is_multi_input; is_variance_scalable = true, is_skip_connection = false)\n\nSpecification for a node input slot. is_multi_input allows several incoming edges to aggregate into one slot. The muPC flags (is_variance_scalable, is_skip_connection) are carried for Phase C; skip-connection slots must NOT be variance-scalable (they are an identity bypass counting toward muPC depth L).\n\n\n\n\n\n","title":"FabricPC.SlotSpec"},{"category":"type","location":"api/#FabricPC.SlotRef","page":"API","text":"SlotRef(node, slot)\n\nA reference to a particular input slot of node, used when wiring an edge to a non-default slot. slot(node, name) is the ergonomic constructor.\n\n\n\n\n\n","title":"FabricPC.SlotRef"},{"category":"type","location":"api/#FabricPC.Edge","page":"API","text":"Edge(source, target)        # wires to target's default \"in\" slot\nEdge(source, ref::SlotRef)  # wires to a specific slot\n\nA directed edge source → target:slot.\n\n\n\n\n\n","title":"FabricPC.Edge"},{"category":"function","location":"api/#FabricPC.slot","page":"API","text":"slot(node, name) -> SlotRef\n\nCreate a SlotRef to node's name slot, validating the slot exists.\n\n\n\n\n\n","title":"FabricPC.slot"},{"category":"type","location":"api/#FabricPC.AbstractNode","page":"API","text":"AbstractNode\n\nEvery node descriptor subtypes this (Linear, IdentityNode, SkipConnection, LinearResidual, TransformerBlock, MhaResidualNode, LnMlp1Node, Mlp2ResidualNode, EmbeddingNode, VocabProjectionNode, StorkeyHopfield). Methods (forward, initialize_params, forward_and_latent_grads, forward_and_weight_grads, …) dispatch on the concrete node type — the Julia analogue of upstream's node_class static-method dispatch.\n\n\n\n\n\n","title":"FabricPC.AbstractNode"},{"category":"type","location":"api/#FabricPC.Linear","page":"API","text":"Linear(shape, name; activation = IdentityActivation(), energy = GaussianEnergy(),\n       use_bias = true, flatten_input = false,\n       weight_init = NormalInitializer(), latent_init = NormalInitializer())\n\nLinear transformation node: z_mu = activation(Σ_e xₑ Wₑ + b). Single multi-input slot \"in\"; multiple incoming edges sum. Implements local predictive-coding learning via explicit Gaussian gradients.\n\nThe weight_init default is NormalInitializer (matching upstream LinearExplicitGrad, not Linear's Kaiming) because our node IS the explicit path. A node with no incoming edges (in_degree == 0) is a pure source: it holds no parameters and contributes no gradients.\n\n\n\n\n\n","title":"FabricPC.Linear"},{"category":"type","location":"api/#FabricPC.IdentityNode","page":"API","text":"IdentityNode(shape, name; activation = IdentityActivation(),\n             energy = GaussianEnergy(), latent_init = NormalInitializer(),\n             scale = 1.0)\n\nIdentity passthrough node: z_mu = scale * Σ_e xₑ. Single multi-input slot \"in\"; no weights or biases.\n\n\n\n\n\n","title":"FabricPC.IdentityNode"},{"category":"type","location":"api/#FabricPC.SkipConnection","page":"API","text":"SkipConnection(shape, name; activation = IdentityActivation(),\n               energy = GaussianEnergy(), latent_init = NormalInitializer())\n\nResidual/skip summation node: z_mu = Σ_e xₑ. Single multi-input slot \"in\" that is NOT muPC-variance-scaled. No weights or biases.\n\n\n\n\n\n","title":"FabricPC.SkipConnection"},{"category":"type","location":"api/#FabricPC.LinearResidual","page":"API","text":"LinearResidual(shape, name; activation = IdentityActivation(),\n               energy = GaussianEnergy(), use_bias = true, flatten_input = false,\n               weight_init = NormalInitializer(), latent_init = NormalInitializer())\n\nResidual block node z_mu = activation(Σ_in xₑWₑ + b) + Σ_skip xₛ. Slots \"in\" (weighted, muPC-scalable) and \"skip\" (identity bypass, unscaled).\n\n\n\n\n\n","title":"FabricPC.LinearResidual"},{"category":"function","location":"api/#FabricPC.get_slots","page":"API","text":"get_slots(node::AbstractNode) -> Dict{String,SlotSpec}\n\nNamed input slots for this node type (e.g. \"in\", \"skip\"/\"residual\" for nodes with a decoupled bypass edge). Dispatches on the concrete node type — every node implements its own; there is no generic fallback.\n\n\n\n\n\n","title":"FabricPC.get_slots"},{"category":"function","location":"api/#FabricPC.compute_mu","page":"API","text":"compute_mu(node, params, inputs) -> z_mu::Matrix{Float32}\n\nPure numeric prediction of this node's latent from its parameters and inputs — concrete arrays only (no NodeState). Custom / non-linear / transformer nodes implement THIS one method; forward, energy_kernel, and both gradient methods are then generic. Must be expressible through Enzyme-differentiable array ops. Multiple incoming edges are combined here (e.g. summed) exactly as in forward.\n\n\n\n\n\n","title":"FabricPC.compute_mu"},{"category":"function","location":"api/#FabricPC.energy_kernel","page":"API","text":"energy_kernel(node, params, inputs, z_latent) -> Float32\n\nScalar total node energy Σ E(z_latent, compute_mu(node, params, inputs)). This is the function Enzyme differentiates (w.r.t. params for weight grads; w.r.t. inputs/z_latent for inference grads). Generic in terms of compute_mu. params weights/biases are SoA (NamedTuple-backed) so Enzyme can accumulate into them.\n\n\n\n\n\n","title":"FabricPC.energy_kernel"},{"category":"type","location":"api/#FabricPC.TransformerBlock","page":"API","text":"TransformerBlock(shape, name; num_heads = 4, activation = IdentityActivation(),\n                 energy = GaussianEnergy(), internal_activation = GeluActivation(),\n                 ff_dim = 4*embed_dim, use_rope = true,\n                 weight_init = NormalInitializer(), latent_init = NormalInitializer())\n\nA pre-norm transformer block as a single PC node. shape = (seq_len, embed_dim); embed_dim must be divisible by num_heads. Single multi-input-free \"in\" slot. causal=true masks future keys (autoregressive). Local PC gradients via the Enzyme autodiff seam (using Enzyme to activate).\n\n\n\n\n\n","title":"FabricPC.TransformerBlock"},{"category":"type","location":"api/#FabricPC.MhaResidualNode","page":"API","text":"MhaResidualNode(shape, name; num_heads=4, use_rope=true, causal=false, energy=GaussianEnergy(),\n                weight_init=NormalInitializer(), latent_init=NormalInitializer())\n\nMulti-head self-attention with a pre-norm residual: z_mu = x + W_o·MHA(LayerNorm(x)). Two input slots: \"in\" (the residual/main path) and \"skip\" (optional, decoupled bypass — falls back to \"in\" if unwired). Seam-based (implements only compute_mu; local PC gradients via Zygote or Enzyme). Part of the decomposed transformer_v2 family — pairs with LnMlp1Node and Mlp2ResidualNode.\n\n\n\n\n\n","title":"FabricPC.MhaResidualNode"},{"category":"type","location":"api/#FabricPC.LnMlp1Node","page":"API","text":"LnMlp1Node(shape, name; activation=GeluActivation(), energy=GaussianEnergy(),\n          weight_init=NormalInitializer(), latent_init=NormalInitializer())\n\nFirst MLP layer with a leading LayerNorm: z_mu = activation(LayerNorm(x) · W_ff1 + b_ff1), shape (S, ff_dim). Single \"in\" slot (from MhaResidualNode). Seam-based (implements only compute_mu). Pairs with Mlp2ResidualNode for the second half of the FFN.\n\n\n\n\n\n","title":"FabricPC.LnMlp1Node"},{"category":"type","location":"api/#FabricPC.Mlp2ResidualNode","page":"API","text":"Mlp2ResidualNode(shape, name; energy=GaussianEnergy(), weight_init=NormalInitializer(),\n                 latent_init=NormalInitializer())\n\nSecond MLP layer with a residual add: z_mu = residual + (mlp1_in · W_ff2 + b_ff2). TWO input slots: \"in\" (from LnMlp1Node, shape (S, ff_dim)) and \"residual\" (the skip from MhaResidualNode, shape (S, embed_dim)) — the residual edge carries no weight, an identity Jacobian into z_mu. Seam-based (implements only compute_mu).\n\n\n\n\n\n","title":"FabricPC.Mlp2ResidualNode"},{"category":"type","location":"api/#FabricPC.EmbeddingNode","page":"API","text":"EmbeddingNode(shape, name; vocab_size, energy=GaussianEnergy(),\n              weight_init=NormalInitializer(; std=0.02), latent_init=NormalInitializer())\n\nDiscrete token-id lookup: z_mu[b,t,:] = embeddings[idx[b,t], :]. idx is a (B, S) array of 1-based (Float32-encoded) token indices in 1:vocab_size. Uses EXPLICIT (not autodiff-seam) gradients: the weight gradient is a scatter-add of ∂E/∂z_mu into embedding rows by token id; the input gradient is zero (no gradient through a discrete index). Port of transformer_v2.EmbeddingNode.\n\n\n\n\n\n","title":"FabricPC.EmbeddingNode"},{"category":"type","location":"api/#FabricPC.VocabProjectionNode","page":"API","text":"VocabProjectionNode(shape, name; activation=SoftmaxActivation(), energy=CrossEntropyEnergy(),\n                    weight_init=XavierInitializer(), latent_init=NormalInitializer())\n\nFinal embedding→vocabulary projection: z_mu = activation(x · W_out + b_out), shape (S, vocab_size). Defaults to Softmax + CrossEntropy (matches upstream transformer_v2.VocabProjectionNode's DEFAULT_ACTIVATION/DEFAULT_ENERGY) — z_mu is a probability distribution over the vocab axis (the LAST axis), not raw logits. Seam-based (implements only compute_mu).\n\n\n\n\n\n","title":"FabricPC.VocabProjectionNode"},{"category":"function","location":"api/#FabricPC.transformer_lm","page":"API","text":"transformer_lm(; seq_len, vocab_size, embed_dim = 32, num_heads = 4,\n               num_blocks = 1, ff_dim = nothing, use_rope = true,\n               infer_steps = nothing, eta_infer = 0.1,\n               scaling = nothing) -> GraphStructure\n\nAssemble a predictive-coding transformer language model and return its GraphStructure (build params with initialize_params(structure, rng)).\n\nembed_dim must be divisible by num_heads. infer_steps defaults to the upstream heuristic 3 * (2*num_blocks + 2). Task map is (x = input, y = output): clamp a (B, seq_len) integer token batch on x; the output node's z_mu is the (B, seq_len, vocab_size) next-token logits.\n\n\n\n\n\n","title":"FabricPC.transformer_lm"},{"category":"type","location":"api/#FabricPC.StorkeyHopfield","page":"API","text":"StorkeyHopfield(shape, name; activation = TanhActivation(), energy = GaussianEnergy(),\n                hopfield_strength = nothing, use_bias = true, enforce_symmetry = true,\n                zero_diagonal = false, weight_init = XavierInitializer(),\n                latent_init = NormalInitializer())\n\nHopfield associative-memory PC node. shape = (D,). hopfield_strength = nothing makes the strength learnable (softplus-constrained ≥ 0, init so s=1); a number fixes it. W (D×D, stored under the input edge key) participates in BOTH the prediction (probe·W) and the attractor energy. Local PC gradients via the Enzyme seam (using Enzyme).\n\n\n\n\n\n","title":"FabricPC.StorkeyHopfield"},{"category":"type","location":"api/#FabricPC.NodeParams","page":"API","text":"NodeParams{W<:SoA, B<:SoA}\n\nLearnable parameters of one node: named weight matrices (SoA) + named biases (SoA). NodeParams(weights::AbstractDict, biases::AbstractDict) coerces from plain Dicts.\n\n\n\n\n\n","title":"FabricPC.NodeParams"},{"category":"type","location":"api/#FabricPC.GraphParams","page":"API","text":"All learnable parameters, keyed by node name.\n\n\n\n\n\n","title":"FabricPC.GraphParams"},{"category":"type","location":"api/#FabricPC.NodeState","page":"API","text":"Dynamic per-node state during inference. energy is per-sample, shape (batch,); the rest are (batch, features...). latent_grad accumulates dE/dz_latent across the node's own self-grad and downstream successors' contributions.\n\nNo pre_activation field: it is computed and consumed WITHIN a single forward, never stored. Only the explicit-gradient path needs it (for f'(pre) in pre_grad), and it gets it directly from _forward_with_preact. Mirrors upstream b6f64ad, which cut per-node inference state from 5 (batch, features...) tensors to 4 — a real saving in the in-place lane, which allocates one of these per node per step.\n\nPARAMETRIC (J-06). Fields were ::Any, which boxed EVERY latent operand: @code_warntype showed forward(::Linear) inferring to Tuple{Any, NodeState} even for a concrete node, and run_inference on the MNIST-MLP allocating 6565 allocs / 208 MB per call (Profile.Allocs: 360 Memory{NodeState}, 320 NodeState, 240 boxed Tuple{...}). One INDEPENDENT type parameter per field (not a shared A) so every construction site auto-infers a fully-concrete type with no edit, and the two test sites that build partial states with nothing still work (each field free to be Nothing). ::NodeState dispatch annotations and Dict{String,NodeState} fields match the UnionAll unchanged. JET.report_call was 0 errors before this — the code was CORRECT; the instability was purely perf, so bit-identity against the tiers is the gate.\n\n\n\n\n\n","title":"FabricPC.NodeState"},{"category":"type","location":"api/#FabricPC.GraphState","page":"API","text":"Dynamic state of the whole graph: per-node NodeState + batch size.\n\n\n\n\n\n","title":"FabricPC.GraphState"},{"category":"type","location":"api/#FabricPC.NodeInfo","page":"API","text":"Topology metadata for one node, attached at graph-build time. Activation / energy / initializers live on the node descriptor itself (read via dispatch); NodeInfo carries only what the loops need: degrees, edge keys, slots, and the (Phase C) muPC scaling_config (always nothing in v0).\n\n\n\n\n\n","title":"FabricPC.NodeInfo"},{"category":"type","location":"api/#FabricPC.EdgeInfo","page":"API","text":"Metadata for one edge: key \"source->target:slot\".\n\n\n\n\n\n","title":"FabricPC.EdgeInfo"},{"category":"type","location":"api/#FabricPC.SlotInfo","page":"API","text":"Metadata for one input slot (resolved at graph-build time).\n\n\n\n\n\n","title":"FabricPC.SlotInfo"},{"category":"type","location":"api/#FabricPC.GraphStructure","page":"API","text":"Static graph topology (compile-time constant). nodes/infos are parallel maps from node name to descriptor / metadata. node_names preserves INSERTION order (the order the inference + learning loops iterate); node_order is the topological order used only by feedforward state init. config is a NamedTuple (inference, graph_state_initializer).\n\n\n\n\n\n","title":"FabricPC.GraphStructure"},{"category":"type","location":"api/#FabricPC.MuPCConfig","page":"API","text":"MuPCConfig(; include_output = false, terminal_input_variance = 1.0)\n\nmuPC configuration passed as the scaling argument to graph(). include_output adds output nodes (outdegree 0) to scaling with the O(1/N) output formula — use true with MSE/Gaussian energy, false with softmax+CE. Port of MuPCConfig (deprecated depthmetric/mindepth fields dropped). `terminalinput_variance` is reserved (unused in the simplified formula).\n\n\n\n\n\n","title":"FabricPC.MuPCConfig"},{"category":"type","location":"api/#FabricPC.MuPCScalingFactors","page":"API","text":"Per-node muPC scaling factors, all per-edge dicts keyed by edge key. Edges into non-variance-scalable slots are ABSENT (call-sites treat a missing key as a no-op, preserving dtype). Port of MuPCScalingFactors.\n\n\n\n\n\n","title":"FabricPC.MuPCScalingFactors"},{"category":"function","location":"api/#FabricPC.compute_mupc_scalings","page":"API","text":"compute_mupc_scalings(nodes, infos, edges, config, node_order) -> Dict{String,Any}\n\nPer-node MuPCScalingFactors from graph topology (or nothing for terminal inputs, and for outputs unless include_output). Port of compute_mupc_scalings.\n\n\n\n\n\n","title":"FabricPC.compute_mupc_scalings"},{"category":"type","location":"api/#FabricPC.InferenceSGD","page":"API","text":"InferenceSGD(; eta_infer = 0.1, infer_steps = 20, latent_decay = 0.0)\n\nStandard SGD inference: z -= eta_infer · latent_grad (with optional weight decay z *= 1 - eta_infer·latent_decay).\n\n\n\n\n\n","title":"FabricPC.InferenceSGD"},{"category":"type","location":"api/#FabricPC.InferenceSGDNormClip","page":"API","text":"InferenceSGDNormClip(; eta_infer = 0.1, infer_steps = 20, latent_decay = 0.0,\n                      max_norm = 1.0, eps = 1e-8)\n\nSGD inference with per-sample L2 gradient-norm clipping: z -= eta_infer · clip(latent_grad), clipping each node's latent gradient independently per batch row (||grad|| > max_norm scales it down to max_norm; eps guards the division against a zero norm). Port of InferenceSGDNormClip (inference.py:312-356).\n\n\n\n\n\n","title":"FabricPC.InferenceSGDNormClip"},{"category":"function","location":"api/#FabricPC.run_inference","page":"API","text":"Run the inference loop infer_steps times to convergence. Port of run_inference. Dispatches on structure.config.inference's type via _run_inference_loop: the generic per-step inference_step/compute_new_latent path below for algorithms with no cross-step state (InferenceSGD, InferenceSGDNormClip), or a specialized override for algorithms that need to carry extra state across steps (InferenceSGDMomentum's velocity — see its _run_inference_loop method further down). structure::GraphStructure is not itself parametric on config's type (config::NamedTuple, checked at runtime), so the dispatch is on the extracted inference value, not on structure directly — this is Julia's analogue of upstream's per-algorithm subclass override of run_inference (inference.py's InferenceBase hierarchy).\n\noptimize=false (default) runs the exact stock loop, byte-for-byte unchanged — this is what every conformance test and the whole training path use, so their behaviour is untouched. Passing optimize=true selects the algebraically-equivalent HOIST+PRUNE loop (_run_inference_loop_opt, below) for the two stateless SGD algorithms. That loop is bit-identical on every field the rest of the system ever CONSUMES — all unclamped nodes' z_latent (the actual inference result) and every node's z_mu/error/energy, hence identical downstream weight gradients and energy (compute_local_weight_gradients/get_graph_param_gradient recompute those from the converged state and never read latent_grad). Its ONLY observable divergence is the latent_grad of clamped nodes, which it leaves at zero instead of accumulating the provably-dead value that update_latents skips for clamped nodes anyway. Because a raw conformance dump (Tier C/D) DOES compare that scratch field, the optimization is deliberately opt-in rather than default-on — see test/optimizations/test_inference_optimizations.jl for the bit-identity oracle and _hoistable_nodes/_run_inference_loop_opt for the mechanics. InferenceSGDMomentum ignores the flag (falls through to its own velocity-carrying loop).\n\n\n\n\n\n","title":"FabricPC.run_inference"},{"category":"function","location":"api/#FabricPC.inference_step","page":"API","text":"One inference step: zero grads → forward+accumulate → latent update. Port of inference_step.\n\n\n\n\n\n","title":"FabricPC.inference_step"},{"category":"function","location":"api/#FabricPC.compute_local_weight_gradients","page":"API","text":"compute_local_weight_gradients(params, final_state, structure) -> GraphParams\n\nPer-node explicit local weight gradients computed from the converged inference state. Port of compute_local_weight_gradients.\n\n\n\n\n\n","title":"FabricPC.compute_local_weight_gradients"},{"category":"function","location":"api/#FabricPC.graph","page":"API","text":"graph(nodes, edges, task_map, inference;\n      graph_state_initializer = FeedforwardStateInit(), scaling = nothing) -> GraphStructure\n\nBuild a GraphStructure from node descriptors, Edges, and a TaskMap. Port of graph(). Pass a MuPCConfig as scaling to compute and attach per-node muPC factors (NodeInfo.scaling_config); nothing (default) leaves scaling off.\n\n\n\n\n\n","title":"FabricPC.graph"},{"category":"type","location":"api/#FabricPC.TaskMap","page":"API","text":"TaskMap(; task = node_or_name, …)\n\nMaps task names (e.g. x, y) to node names. Values may be AbstractNode instances or name strings.\n\n\n\n\n\n","title":"FabricPC.TaskMap"},{"category":"function","location":"api/#FabricPC.initialize_params","page":"API","text":"initialize_params(node::ConvNode, rng, node_shape, input_shapes, weight_init) -> NodeParams\n\nKernels (kernel_size…, C_in, C_out) per in-edge; bias (1,…,1, C_out) (rank R+2, broadcasting over batch and every spatial axis). Port of convolutional.py:143-202.\n\nShape validation fires HERE, not at construction — the input shapes it checks against are only known once the graph is wired (upstream's own reasoning, convolutional.py:19-23). channels_preserved=false: a conv sets its own C_out via the kernel.\n\n\n\n\n\nPooling has no learnable parameters. Windowed mode validates HERE — the only place the input shapes are known (upstream pooling.py:102-132); channels_preserved=true because pooling cannot change the channel count. Global mode has no spatial output, so it is skipped (its rank-1 shape is checked at construction instead).\n\n\n\n\n\ninitialize_params(structure, rng) -> GraphParams\n\nInitialize parameters for every node in structure. Port of initialize_params.\n\n\n\n\n\n","title":"FabricPC.initialize_params"},{"category":"type","location":"api/#FabricPC.FeedforwardStateInit","page":"API","text":"Feedforward state initializer: propagate predictions through the DAG. Port of FeedforwardStateInit.\n\n\n\n\n\n","title":"FabricPC.FeedforwardStateInit"},{"category":"function","location":"api/#FabricPC.initialize_graph_state","page":"API","text":"initialize_graph_state(structure, batch_size, rng; clamps, state_init, params) -> GraphState\n\nInitialize the graph state with the chosen (or default) strategy. Port of initialize_graph_state (its float-clamp validation is unnecessary in v0 — all latents are float).\n\n\n\n\n\n","title":"FabricPC.initialize_graph_state"},{"category":"function","location":"api/#FabricPC.get_graph_param_gradient","page":"API","text":"get_graph_param_gradient(params, batch, structure, rng) -> (grads, energy_per_sample, final_state)\n\nRun inference for batch and compute local weight gradients without updating params. batch maps task names (e.g. \"x\", \"y\") to (batch, features) arrays. Port of get_graph_param_gradient.\n\n\n\n\n\n","title":"FabricPC.get_graph_param_gradient"},{"category":"function","location":"api/#FabricPC.train_step","page":"API","text":"train_step(params, batch, structure, lr, rng) -> (params, energy_per_sample, final_state)\n\nOne PC training step with an SGD weight update. Port of train_step.\n\n\n\n\n\n","title":"FabricPC.train_step"},{"category":"function","location":"api/#FabricPC.train_step!","page":"API","text":"train_step!(opt::AdamW, params, batch, structure, rng) -> (params, energy, final_state)\n\nOne PC training step with an AdamW weight update (the muPC training recipe's optimizer). Same inference + local-gradient path as train_step, but the weight update is AdamW instead of plain SGD.\n\n\n\n\n\n","title":"FabricPC.train_step!"},{"category":"function","location":"api/#FabricPC.sgd_update","page":"API","text":"sgd_update(params, grads, lr) -> GraphParams\n\nPlain SGD: param -= lr · grad, walking the GraphParams tree. Nodes with no parameters (empty grad dicts) pass through unchanged. Faithful to optax.sgd + optax.apply_updates.\n\n\n\n\n\n","title":"FabricPC.sgd_update"},{"category":"function","location":"api/#FabricPC.predict","page":"API","text":"predict(params, structure, inputs, rng; output_task = \"y\") -> array\n\nInference-only forward: clamp the given input task(s) (e.g. \"x\"), relax the graph to convergence, and return the output task node's prediction (z_mu). For a classifier, argmax over the feature axis gives the predicted class. Mirrors the inference half of upstream eval_step (single-device, eager).\n\n\n\n\n\n","title":"FabricPC.predict"},{"category":"type","location":"api/#FabricPC.AdamW","page":"API","text":"AdamW(params; lr=0.002, beta1=0.9, beta2=0.999, eps=1e-8, weight_decay=0.0)\n\nAdamW optimizer state for a GraphParams tree (first/second moments + step counter). Faithful to optax.adamw. Defaults match mupc_demo.py (lr 0.002, weight_decay 0.01 set explicitly there).\n\n\n\n\n\n","title":"FabricPC.AdamW"},{"category":"function","location":"api/#FabricPC.step!","page":"API","text":"step!(opt::AdamW, params, grads) -> GraphParams\n\nOne AdamW update. Mutates opt (moments + step); returns the new params.\n\n\n\n\n\n","title":"FabricPC.step!"},{"category":"function","location":"api/#FabricPC.train_pcn","page":"API","text":"train_pcn(params, structure, train_loader, opt; num_epochs = 10,\n          rng = Random.default_rng(), verbose = true,\n          epoch_callback = nothing, iter_callback = nothing)\n    -> (params, iter_energies, epoch_results)\n\nTrain a predictive-coding network with local learning — no backprop. Loops num_epochs (fractional supported) × batches over train_loader (a re-iterable of task→array batches), calling the local train_step!/train_step and accumulating per-batch energy. opt is an AdamW optimizer OR a Real learning rate (plain SGD). Returns the trained params, a [epochs][batches] energy history, and the collected epoch_callback results. Port of train_pcn (single-device path; pmap/multi-GPU deferred).\n\n\n\n\n\n","title":"FabricPC.train_pcn"},{"category":"function","location":"api/#FabricPC.evaluate_pcn","page":"API","text":"evaluate_pcn(params, structure, test_loader; input_task = \"x\",\n             output_task = \"y\", rng = Random.default_rng())\n    -> Dict{String,Float64}\n\nEvaluate a PC classifier (no backprop): for each batch, clamp the input, relax the graph, and argmax the output node's prediction vs the label's argmax → accuracy; also accumulate mean per-sample energy (with input+label clamped). Returns Dict(\"accuracy\" => …, \"energy\" => …). Port of evaluate_pcn (single-device).\n\n\n\n\n\n","title":"FabricPC.evaluate_pcn"},{"category":"type","location":"api/#FabricPC.NaturalGradientDiag","page":"API","text":"NaturalGradientDiag(params; fisher_decay = 0.95, damping = 1e-3)\n\nEMA diagonal-Fisher preconditioner state for a GraphParams tree. Port of scale_by_natural_gradient_diag.\n\n\n\n\n\n","title":"FabricPC.NaturalGradientDiag"},{"category":"type","location":"api/#FabricPC.NaturalGradientLayerwise","page":"API","text":"NaturalGradientLayerwise(; fisher_decay = 0.95, damping = 1e-3)\n\nOne scalar Fisher estimate per parameter tensor (cheaper, often more stable than the diagonal). Port of scale_by_natural_gradient_layerwise. The per-leaf scalar state is built lazily on the first precondition!.\n\n\n\n\n\n","title":"FabricPC.NaturalGradientLayerwise"},{"category":"function","location":"api/#FabricPC.precondition!","page":"API","text":"precondition!(ng::NaturalGradientDiag, grads::GraphParams) -> GraphParams\n\nUpdate the Fisher EMA (f ← decay·f + (1−decay)·g²) and return the preconditioned gradient (g / (f + damping)), elementwise over every weight/bias.\n\n\n\n\n\nprecondition!(ng::NaturalGradientLayerwise, grads::GraphParams) -> GraphParams\n\nUpdate each tensor's scalar Fisher (f ← decay·f + (1−decay)·mean(g²)) and return g / (f + damping).\n\n\n\n\n\n","title":"FabricPC.precondition!"},{"category":"function","location":"api/#FabricPC.create_causal_mask","page":"API","text":"create_causal_mask(seq_len) -> Matrix{Float32}  (seq_len × seq_len)\n\nLower-triangular causal mask: mask[i,j] = 1 if j ≤ i else 0 — position i may attend to 0…i. Port of train_autoregressive.py:29 (jnp.tril(jnp.ones((L, L)))).\n\nNOTE: this is the binary TRAINING/task mask (used via task_map[\"causal_mask\"]). It is distinct from the additive (−1e9) ATTENTION mask _tb_causal_mask in nodes/transformer.jl — do not conflate.\n\n\n\n\n\n","title":"FabricPC.create_causal_mask"},{"category":"function","location":"api/#FabricPC.compute_loss","page":"API","text":"compute_loss(state, targets, output_node; loss_type=:cross_entropy) -> Float32\n\nScalar monitoring loss on the output node's prediction (state.nodes[output_node].z_mu), averaged over the batch. Port of train_autoregressive.py:40.\n\n:cross_entropy — expects one-hot targets: −mean(Σ_v targets · log(pred + 1e-10)), the inner sum over the last (feature/vocab) axis, the mean over the remaining (batch[, seq]) axes.\n:mse — mean((pred − targets)²).\n\n\n\n\n\n","title":"FabricPC.compute_loss"},{"category":"function","location":"api/#FabricPC.train_step_autoregressive","page":"API","text":"train_step_autoregressive(params, opt, batch, structure, rng) -> (params, energy, ce_loss, final_state)\n\nOne PC training step for sequence/autoregressive data: clamp the batch tasks via task_map, relax the graph by inference, apply local weight gradients (opt = an AdamW or a plain-SGD learning rate), and additionally return the output cross-entropy (perplexity metric — NOT used for the update). Port of train_step_autoregressive (train_autoregressive.py:81).\n\nbatch[\"y\"] may be raw integer token ids (B, S) (the loader contract — see _TokenSequenceLoader docstring, dataloader.py) OR pre-shaped one-hot (B, S, V). Raw ids are one-hot-expanded HERE (via _one_hot, vocab size from structure.infos[task_map[\"y\"]].shape[end]) before _pcn_step builds clamps, so the clamped output node — not compute_loss alone — sees the expanded array. Port of the y.ndim/jax.nn.one_hot block (trainautoregressive.py:127-139). DIVERGENCE (intentional): upstream raises unless y.ndim == 2 (it always one-hots); this also passes already-one-hot y through unchanged (ndim already matching the prediction), so callers that pass ready-made one-hot batches (e.g. `testsequencetraining.jl's plain-classifier step) keep working.computelossitself keeps no ndim-check of its own (unlike upstream's, train_autoregressive.py:67-68): expansion happens exactly once, here, socompute_loss` can assume already-shaped targets.\n\nDIVERGENCE (intentional — not a 1:1 transplant): upstream clamps a causal-mask NODE (task_map[\"causal_mask\"], broadcast to (B,1,S,S)). Our TransformerBlock masks INLINE via its causal flag (_tb_causal_mask, fixed at graph construction), so there is no mask node to clamp. We also use the stateful AdamW/lr optimizer rather than threading an optax opt_state.\n\n\n\n\n\n","title":"FabricPC.train_step_autoregressive"},{"category":"function","location":"api/#FabricPC.train_autoregressive","page":"API","text":"train_autoregressive(params, structure, batches, opt, num_epochs, rng;\n                     verbose=true, epoch_callback=nothing) -> (params, iter_energies, epoch_results)\n\nMain PC autoregressive training loop. batches is any iterable of task→array Dicts (each with at least \"x\"/\"y\"). Per epoch, runs train_step_autoregressive over every batch, accumulating the per-batch energy and the CE/perplexity metric; epoch_callback(epoch, params, structure) runs after each epoch. Port of train_autoregressive (train_autoregressive.py:197-331). Eager (no jax.jit).\n\nR-01 (docs/AUDITREGISTER.md section 8) full-driver comparison found this docstring previously MISCHARACTERIZED one upstream feature as an \"unused stub\" alongside a genuinely unused one — see X-06 (docs/AUDITREGISTER.md section 2) for the corrected, complete picture: num_epochs here is Integer-only (upstream's frac/max_batches fractional-epoch truncation, trainautoregressive.py: 238-263, is REAL and reachable, not a stub — unit-tested upstream, `tests/testfabricpc.py:520-549);gradientaccumulationstepsgenuinely IS a dead read (upstream reads it, never uses it again) — the only part of the old docstring's claim that was accurate;itercallback` (per-batch, upstream trainautoregressive.py:298-301) has no equivalent here at all, despite the identical pattern already existing elsewhere in this package (train_pcn, training/train.jl); epoch_callback here takes only (epoch, params, structure) vs upstream's (epoch_idx, params, structure, config, rng_key; energy, ce_loss). All three are currently dormant in this port (no example/test needs them) but fabricpc/tuning/bayesian_tuner.py (C-10, deferred) is a live upstream consumer of both — porting BayesianTuner is currently structurally blocked on this gap, not just stylistically divergent from it.\n\n\n\n\n\n","title":"FabricPC.train_autoregressive"},{"category":"function","location":"api/#FabricPC.generate_autoregressive","page":"API","text":"generate_autoregressive(params, structure, prompt, max_new_tokens, rng;\n                        temperature=1.0, top_k=nothing, top_p=nothing) -> tokens\n\nAutoregressively generate max_new_tokens token indices (1-based) from prompt (a 1-D (seq,) or 2-D (batch, seq) index array). Per step: format the context window for the input node (1-D input-node shape ⇒ raw indices for an EmbeddingNode; 2-D ⇒ one-hot for a Linear), clamp the input only, relax by inference, take the last-position output probabilities, sample via _sample_next, then slide the context window. Returns prompt ++ generated (unbatched if the prompt was 1-D). Port of generate_autoregressive (train_autoregressive.py:407); eager (upstream uses jax.lax.scan + jax.jit), no fixed-size output buffer needed.\n\n\n\n\n\n","title":"FabricPC.generate_autoregressive"},{"category":"function","location":"api/#FabricPC.evaluate_autoregressive","page":"API","text":"evaluate_autoregressive(params, structure, test_loader, rng; debug=false)\n    -> Dict{String,Float64}\n\nEvaluate a trained autoregressive model on held-out data: mean cross-entropy loss, perplexity (exp(loss)), and next-token accuracy, over every batch in test_loader (any iterable of task→array Dicts with \"x\"/\"y\" — e.g. CharDataLoader, examples/char_lm_pc.jl, matching train_autoregressive's own loader contract). debug=true prints the same first-batch diagnostics as upstream (per-token CE, per-token intrinsic perplexity, probability mass on the correct token). Port of evaluate_autoregressive (trainautoregressive.py:595-710). DIVERGENCE: no config/`usecausalmaskparameter — the upstream causal-mask clamp branch this fed is dead code in this port (seeevalstepautoregressiveabove);rngis threaded sequentially (Julia's mutable-state RNG idiom, matchingtrain_autoregressive's own signature) rather than pre-split viajax.random.split(a JIT-specific pattern with no Julia-eager equivalent); nojax.jit` wrapper.\n\n\n\n\n\n","title":"FabricPC.evaluate_autoregressive"},{"category":"type","location":"api/#FabricPC.CompiledPlan","page":"API","text":"CompiledPlan(structure)\n\nStatic lowering of a GraphStructure for the Dict-free path. Node position = insertion order (the order the eager loops iterate). Per position it stores the node, its NodeInfo, and its in-edges as parallel vectors of (source position, slot). order_pos is the topological order (positions) for feedforward init.\n\n\n\n\n\n","title":"FabricPC.CompiledPlan"},{"category":"function","location":"api/#FabricPC.to_flat_state","page":"API","text":"to_flat_state(plan::CompiledPlan, state::GraphState) -> Vector{NodeState}\n\nBridge GraphState (a Dict{String,NodeState}) to a position-indexed Vector{NodeState}, ordered to match plan.names.\n\n\n\n\n\n","title":"FabricPC.to_flat_state"},{"category":"function","location":"api/#FabricPC.flat_run_inference","page":"API","text":"flat_run_inference(plan, fparams, fstate, clamped; optimize=false) -> Vector{NodeState}\n\nDict-free run_inference: infer_steps flat inference steps. clamped[i] marks clamped positions. Reproduces the eager loop exactly (validated in testjitflat).\n\noptimize=true selects the positional HOIST+PRUNE loop (_flat_run_inference_opt), the flat-lane twin of run_inference(...; optimize=true) in core/inference.jl §28 — same two exact optimizations (hoist a position whose in-sources are all clamped; skip gradient accumulation into clamped positions), same bit-identity on all consumed fields. Bundled here to measure the optimization on the DICT-FREE eager path (isolating the algorithmic win from the Dict/string-key overhead the core eager path carries — see docs/decisions.md §28's three-way decomposition).\n\n\n\n\n\n","title":"FabricPC.flat_run_inference"},{"category":"function","location":"api/#FabricPC.flatten_param_arrays","page":"API","text":"Flatten per-node params to a flat Tuple of arrays + a static layout that repack_params uses to rebuild them. The tuple is what Reactant.@compile traces; the layout (integer indices) is a compile-time constant.\n\n\n\n\n\n","title":"FabricPC.flatten_param_arrays"},{"category":"function","location":"api/#FabricPC.repack_params","page":"API","text":"Rebuild Vector{FlatNodeParams} from a flat array tuple + layout. Built inside the traced region, so the (parametric) FlatNodeParams hold tracer arrays.\n\n\n\n\n\n","title":"FabricPC.repack_params"},{"category":"function","location":"api/#FabricPC.state_from_latents","page":"API","text":"Build a Vector{NodeState} from per-node z_latents; other fields are zero placeholders (recomputed in inference step 1, so unused — matches the eager init).\n\n\n\n\n\n","title":"FabricPC.state_from_latents"},{"category":"function","location":"api/#FabricPC.jit_inference_runner","page":"API","text":"jit_inference_runner(arr_tuple, zl, plan, layout, clamped) -> Tuple of z_latents\n\nReactant-traceable entry point: takes only Tuples of arrays (params flattened via flatten_param_arrays, plus per-node zlatents), runs `flatruninference`, and returns the new zlatents tuple. plan/layout/clamped are compile-time-static — capture them in a closure before Reactant.@compile (see FabricPCReactantExt).\n\n\n\n\n\n","title":"FabricPC.jit_inference_runner"},{"category":"function","location":"api/#FabricPC.compile_inference","page":"API","text":"compile_inference(structure, params, clamps; batch) -> callable\n\nJIT-compile the predictive-coding inference loop via Reactant/XLA (≈9× faster than eager on the MNIST-shaped MLP). The returned object maps (params, init_state) -> Vector of converged per-node z_latent arrays (node order = structure.node_names). The compiled thunk is specialized to the param shapes, batch, and which nodes are clamped — recompile if those change.\n\nRequires using Reactant (implemented by the FabricPCReactantExt extension).\n\n\n\n\n\n","title":"FabricPC.compile_inference"},{"category":"section","location":"getting_started/quickstart/#Quickstart","page":"Quickstart","text":"A minimal predictive-coding classifier: x(4) → h(8, Tanh) → y(3, Softmax/CrossEntropy), trained with local PC learning (no backprop).\n\nusing FabricPC\nusing Random\n\nrng = MersenneTwister(0)\n\n# Synthetic separable data: 3 classes in 4-d space.\nB = 32\nlabels = rand(rng, 1:3, B)\nx = Float32.(randn(rng, 4, B)') .+ Float32.(labels' .* 2)   # (B, 4), class-shifted\ny = Float32[labels[b] == c ? 1 : 0 for b in 1:B, c in 1:3]  # one-hot, (B, 3)\n\n# Build the graph.\nxn = Linear((4,), \"x\")\nhn = Linear((8,), \"h\"; activation=TanhActivation())\nyn = Linear((3,), \"y\"; activation=SoftmaxActivation(), energy=CrossEntropyEnergy())\nstructure = graph(\n    [xn, hn, yn], [Edge(xn, hn), Edge(hn, yn)],\n    TaskMap(; x=xn, y=yn),\n    InferenceSGD(; eta_infer=0.1, infer_steps=20)\n)\nparams = initialize_params(structure, MersenneTwister(1))\n\n# Train: each step clamps (x, y), relaxes the graph by local inference, then\n# applies a local (Hebbian-like) weight update — no backprop anywhere.\nbatch = Dict(\"x\" => x, \"y\" => y)\nfor step in 1:100\n    global params\n    params, energy, _ = train_step(params, batch, structure, 0.05, rng)\n    step % 20 == 0 && @info \"train_step\" step energy\nend\n\n# Predict on a fresh batch (only \"x\" clamped; \"y\" relaxes freely to the model's\n# best guess).\npred = predict(params, structure, Dict(\"x\" => x), rng)\naccuracy = sum(argmax(pred[b, :]) == labels[b] for b in 1:B) / B\n@info \"accuracy\" accuracy","title":"Quickstart"},{"category":"section","location":"getting_started/quickstart/#What-just-happened","page":"Quickstart","text":"Inference (inner loop): with x/y clamped, InferenceSGD relaxes every other node's latent (h, here) by local gradient descent on its own prediction error — infer_steps times.\nLearning (outer loop): train_step takes the relaxed state and applies a local weight update per edge — each edge only needs its own source activation and its target's local error, never a gradient that's been backpropagated through the rest of the network.\nPrediction: predict clamps only the input and lets the output node's latent relax to the network's own prediction.","title":"What just happened"},{"category":"section","location":"getting_started/quickstart/#Next-steps","page":"Quickstart","text":"Real data: see examples/mnist_pc.jl in the repo (IDX-format MNIST, no external dataset dependency) or examples/char_lm_pc.jl (character-level language modeling on Tiny Shakespeare).\nmuPC scaling for deep/wide graphs: pass scaling=MuPCConfig(...) to graph — see Architecture.\nTransformers: TransformerBlock (monolithic) or the decomposed MhaResidualNode/LnMlp1Node/Mlp2ResidualNode family — both trained by the same local PC rule, no backprop.\nCompiling inference to XLA: JIT with Reactant.","title":"Next steps"},{"category":"section","location":"getting_started/jit/#JIT-with-Reactant","page":"JIT with Reactant","text":"FabricPC's inference loop (the inner relaxation loop — not weight training) can be compiled to XLA via Reactant.jl, bit-exact to the eager path and substantially faster. This is opt-in: Reactant is a weak dependency, loaded via the FabricPCReactantExt package extension.\n\nusing FabricPC\nusing FabricPC: compile_inference\nusing Reactant   # triggers FabricPCReactantExt\n\nstructure = graph([xn, hn, yn], [Edge(xn, hn), Edge(hn, yn)],\n    TaskMap(; x=xn, y=yn), InferenceSGD(; eta_infer=0.1, infer_steps=20))\nparams = initialize_params(structure, rng)\nclamps = Dict{String, Any}(\"x\" => x, \"y\" => y)\n\nci = compile_inference(structure, params, clamps; batch=size(x, 1))  # traces + compiles once\nz_latents = ci(params, init_state)                                   # run the compiled thunk\n\nThe returned callable maps (params, init_state) -> Vector of converged per-node z_latent arrays (node order = structure.node_names). It's specialized to the param shapes, batch, and which nodes are clamped — recompile if any of those change. See examples/jit_inference.jl for a runnable benchmark against the eager path.","title":"JIT with Reactant"},{"category":"section","location":"getting_started/jit/#What's-compiled-today,-and-what-isn't","page":"JIT with Reactant","text":"Forward inference (the relaxation loop, no weight updates) — works, for the node types Linear, IdentityNode, SkipConnection, LinearResidual, and — as of this writing — TransformerBlock's forward pass (as the graph's unclamped terminal node; using TransformerBlock as an interior or clamped node isn't compiled yet — see below).\nWeight-gradient compilation (compiling the full training step, not just inference) — not implemented. compute_local_weight_gradients remains eager/Dict-based.\nTransformerBlock's backward pass in the compiled lane — not implemented. The forward kernel (_tb_block_flat) is wired in and validated bit-identical to eager compute_mu, but there's no compiled gradient for it yet.\nDecomposed transformer nodes (MhaResidualNode/LnMlp1Node/ Mlp2ResidualNode/EmbeddingNode/VocabProjectionNode) and StorkeyHopfield — no compiled-lane support at all yet.","title":"What's compiled today, and what isn't"},{"category":"section","location":"getting_started/jit/#Why-gradients-are-the-hard-part","page":"JIT with Reactant","text":"Reactant uses Enzyme-MLIR internally for autodiff under @compile — a different thing from loading the Enzyme package eagerly for the Phase-D seam, despite the shared name (see docs/decisions.md §23 for the full backend map). There's a real, reproduced conflict: loading eager Enzyme in the same process as a Reactant+Enzyme @compile poisons the compile. Working around this — and extending compiled gradients past the current closed-form node types — is open, tracked as J-01/J-02 in docs/AUDIT_REGISTER.md section 5.","title":"Why gradients are the hard part"},{"category":"section","location":"getting_started/architecture/#Architecture","page":"Architecture","text":"","title":"Architecture"},{"category":"section","location":"getting_started/architecture/#Core-abstractions","page":"Architecture","text":"Node — owns an output latent z_latent, computes a top-down prediction z_mu from incoming edges, derives error = z_latent − z_mu, contributes a local energy. Single output; named input slots (e.g. \"in\", \"skip\").\nEdge — connects a source node to a target node's slot, keyed \"src->tgt:slot\".\nGraphStructure (static topology) / GraphState (dynamic latents) / GraphParams (weights) — the three pytree-equivalents everything else is built from.\nmuPC — μP (maximal-update parameterization) generalized to predictive coding over arbitrary DAGs: per-edge forward/gradient scaling factors derived from graph topology (slot in-degree, residual depth, fan-in, activation gain) keep activations/errors/gradients O(1) at any width/depth. This is the research contribution the port is built around (Innocenti et al., arXiv:2505.13124; Yang et al. Depth-μP).","title":"Core abstractions"},{"category":"section","location":"getting_started/architecture/#The-key-architectural-property:-no-backprop,-and-v0-needs-no-autodiff-at-all","page":"Architecture","text":"Predictive coding performs bilevel optimization:\n\nInference (inner loop): z ← z·(1 − η·decay) − η·latent_grad, where latent_grad is assembled locally — each node pushes its own prediction-error gradient back to its in-neighbors only.\nLearning (outer loop): local Hebbian-like weight gradients computed from the converged (relaxed) state.\n\nNeither loop ever backpropagates a gradient through the whole network. The closed-form nodes (Linear, IdentityNode, SkipConnection, LinearResidual) compute both gradients analytically — no reverse-mode AD anywhere in that path:\n\nself_grad       = precision · (z_latent − z_mu)\ngain_mod_error  = error · f'(pre_activation)\ninput_grad[e]   = −(gain_mod_error · Wₑᵀ)\ndW[e]           = −(inputₑᵀ · gain_mod_error)\ndb              = −Σ_batch gain_mod_error","title":"The key architectural property: no backprop, and v0 needs no autodiff at all"},{"category":"section","location":"getting_started/architecture/#The-Phase-D-autodiff-seam","page":"Architecture","text":"Nodes whose local gradient isn't hand-derivable in closed form (attention, Storkey-Hopfield's attractor energy) implement only compute_mu — the forward prediction — and get forward_and_latent_grads/ forward_and_weight_grads for free via reverse-mode AD over that one function (Zygote or Enzyme, docs/decisions.md §19/§23). This is still local PC — the seam differentiates one node's own compute_mu, never the whole network — not a backdoor into backprop.","title":"The Phase-D autodiff seam"},{"category":"section","location":"getting_started/architecture/#Execution-layers","page":"Architecture","text":"Four distinct execution paths run the same PC computation (docs/decisions.md §22 has the full writeup):\n\nLayer 0 — eager, Dict-based. The correctness reference; what every other layer (and upstream conformance testing) validates against.\nLayer 1 — flat/positional. Position-indexed Vectors, no Dicts in the hot loop — the intermediate form Reactant traces. Validated bit-identical to Layer 0.\nLayer 2 — Reactant/XLA compiled. Layer 1 traced through Reactant.@compile. Forward-inference JIT works today; compiled gradients are blocked on an architectural conflict (Enzyme eager vs. Reactant-compiled Enzyme in one process) — see JIT with Reactant.\nLayer 3 — muPC. Not a separate execution path — a per-edge scaling applied at any of the layers above.\n\nRule: each layer is validated against the one directly below it, never against upstream directly except Layer 0. A failure in Layer 1 means \"the flat lowering broke,\" not \"the port diverged from upstream.\"","title":"Execution layers"},{"category":"section","location":"getting_started/architecture/#Conformance-vs.-upstream","page":"Architecture","text":"A fixture-based harness (docs/AUDIT_REGISTER.md section 6) verifies this port numerically against the pinned upstream JAX checkout at two levels so far: primitives (activations, energies, RoPE, LayerNorm — \"Tier A\") and node-level forward + gradients, including the muPC-scaled case (\"Tier B\"). Loop-level and end-to-end tiers are planned.","title":"Conformance vs. upstream"},{"category":"section","location":"getting_started/installation/#Installation","page":"Installation","text":"FabricPC.jl requires Julia 1.12+. It isn't registered yet — install it as a dev/path dependency from the GitHub repo:\n\nusing Pkg\nPkg.develop(url=\"https://github.com/CognitiveSubstratesAI/FabricPC\")\n\nor, working from a local clone:\n\njulia --project=. -e 'using Pkg; Pkg.instantiate(); Pkg.test()'","title":"Installation"},{"category":"section","location":"getting_started/installation/#Optional-autodiff-backends","page":"Installation","text":"The core closed-form nodes (Linear, IdentityNode, SkipConnection, LinearResidual) need no autodiff package at all. Nodes that use the Phase-D autodiff seam (transformer/attention, StorkeyHopfield) need exactly one of:\n\nusing Zygote   # the production seam backend — works on the full attention block\n\nor\n\nusing Enzyme   # opt-in, simple/dense nodes only — see docs/decisions.md §19\n\nLoading both in the same Julia session raises an error (_register_ad_backend!, see F-04 in docs/AUDIT_REGISTER.md) rather than silently letting one backend's methods shadow the other's.","title":"Optional autodiff backends"},{"category":"section","location":"getting_started/installation/#JIT-(optional)","page":"Installation","text":"The Reactant/XLA-compiled inference path needs:\n\nusing Reactant\n\nSee JIT with Reactant.","title":"JIT (optional)"},{"category":"section","location":"getting_started/installation/#Development","page":"Installation","text":"Before committing any .jl change, run the Blue formatter (CI enforces it, pinned to JuliaFormatter 2.5.2):\n\njulia -e 'using JuliaFormatter; format(\".\")'","title":"Development"},{"category":"section","location":"#FabricPC.jl","page":"Home","text":"A Julia port of FabricPC (upstream Python/JAX by Matthew Behrend, MIT) — a flexible, graph-based predictive-coding (PC) training framework.\n\nThis is Layer 2 of the NGC Julia stack:\n\nLayer Package Role\n0 NGCSimLib substrate — Component / Compartment / Context / Process\n1 NGCLearn biophysical component zoo\n2 FabricPC (this package) predictive-coding graph training framework + muPC\n\nFabricPC is a standalone package — it does not depend on NGCSimLib/NGCLearn. It is a parallel graph abstraction (Nodes / Edges→slots) rather than NGCSimLib's Component/Compartment substrate; composition between the two stacks is a deferred, optional concern.","title":"FabricPC.jl"},{"category":"section","location":"#What-is-predictive-coding?","page":"Home","text":"PC is a biologically-motivated learning framework that performs bilevel optimization: an inner loop infers latent activations by minimizing local prediction errors, and an outer loop updates weights via local (Hebbian-like) rules. The outer loop does not backpropagate through the inner loop — gradients are local and analytic.\n\nFabricPC builds PC networks from:\n\nNodes — each owns a latent z_latent, predicts z_mu from incoming edges, derives error = z_latent − z_mu, contributes a local energy.\nEdges — connect a source node to a target node's named slot.\nmuPC — μP-style per-edge scaling (from graph topology) that keeps activations, errors, and gradients O(1) at arbitrary width and depth.","title":"What is predictive coding?"},{"category":"section","location":"#Status-—-substrate-essentially-complete","page":"Home","text":"Design + phase history are in Architecture and decisions.md (the load-bearing porting decisions, read before extending). Shipped:\n\nCore PC engine — closed-form explicit-gradient nodes (Linear, IdentityNode, SkipConnection, LinearResidual) need no reverse-mode autodiff: PC's local learning rule is closed-form, so a minimal trainable graph is autodiff-free.\nmuPC scaling (MuPCConfig) — μP-style per-edge scaling that keeps activations/errors/gradients O(1) across width and depth (residual-depth-aware).\nPhase-D autodiff seam — a node that implements only compute_mu gets its local PC gradients for free via Zygote or Enzyme (exactly ONE backend per session). This is what makes transformer/attention/Storkey-Hopfield nodes expressible without hand-derived gradients; it is still pure PC, never backprop through the network.\nNode set: Linear/Identity/SkipConnection/LinearResidual (closed-form), TransformerBlock (monolithic PC-transformer with RoPE + causal masking), the decomposed transformer_v2 node family (MhaResidualNode/LnMlp1Node/Mlp2ResidualNode/ EmbeddingNode/VocabProjectionNode — PC at every sub-component), StorkeyHopfield (composite PC + associative-memory energy).\nTraining: SGD, AdamW, and InferenceSGDNormClip (the muPC training recipe needs an adaptive optimizer), causal autoregressive next-token training (train_autoregressive), held-out evaluation (evaluate_autoregressive), natural-gradient (Fisher) preconditioners.\nReactant/XLA JIT — the inference path, and TransformerBlock's forward pass, compile to XLA via Reactant, validated bit-identical to eager.\nConformance-tested against upstream JAX — a fixture-based harness (docs/AUDIT_REGISTER.md section 6) verifies primitives (Tier A) and node-level forward+gradients (Tier B) numerically match the pinned upstream checkout, not just internal self-consistency.\nNot ported (deliberately): a backprop training baseline (train_backprop.py — PC needs no backprop, so this stays the never-ported anti-thesis reference), multi-GPU/pmap, spatial convolution nodes, dashboards/tuning infra. See decisions.md for the full, current list of what's deferred and why.","title":"Status — substrate essentially complete"},{"category":"section","location":"#Where-to-go-next","page":"Home","text":"Installation / Quickstart — get a PC graph training in a few lines.\nArchitecture — the Node/Edge/slot graph model, the inference/learning loop, and how muPC scaling fits in.\nJIT with Reactant — compiling the inference loop to XLA.\nAPI Reference — full reference.","title":"Where to go next"},{"category":"section","location":"#License","page":"Home","text":"MIT. A Julia port of FabricPC (Matthew Behrend), also MIT. See LICENSE.","title":"License"}]
}
