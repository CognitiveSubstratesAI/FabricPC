# Pinned `pip freeze` of the fixture-generation venv (.venv-fixtures) used by every
# scripts/generate_tier_*.py in this repo, captured 2026-07-14. Regenerate via:
#   source <path-to>/.venv-fixtures/bin/activate && pip freeze > scripts/requirements-fixtures.txt
#
# WHY THIS MATTERS (not just hygiene): Tier D's entire conclusion (docs/AUDIT_REGISTER.md
# section 6, docs/decisions.md #24) rests on MEASURED XLA numerical behavior -- the 1.86e-5
# fori_loop-vs-eager gap, the 9.06e-4 endpoint divergence, eta*=0.0176. XLA's CPU fusion/
# reassociation behavior is not guaranteed stable across jaxlib releases; regenerating the
# Tier A-D fixtures against a different jaxlib could shift these margins and re-litigate the
# derivation against a moved baseline. This file is the record of exactly which jaxlib (and
# every other fixture-venv package) produced the numbers currently checked into
# test/conformance/fixtures/*.npz and cited in the register/decisions log.
#
# Upstream pin: the editable install line below records the exact commit
# (trueagi-io/FabricPC@b6f64adf9314863ce665085a92d544807d585819) fixtures were generated
# against -- the authoritative marker, since upstream's own pyproject.toml version string
# ("0.3.1") was last bumped 2026-05-04 and is several merged PRs stale relative to this SHA
# (see docs/AUDIT_REGISTER.md A-01). Updated 2026-07-15: was 316367c6, now b6f64ad -- every
# tier_*.npz in test/conformance/fixtures/ was regenerated against b6f64ad and verified
# BIT-IDENTICAL to the previous arrays apart from the dropped *_pre_activation keys (C-15).
# NOTE this is an EDITABLE install of the LOCAL checkout (~/JuliaAGI/dev-zone/FabricPC): a
# `git pull` there silently re-points the oracle. The SHA here is the record, not a lock.
absl-py==2.5.0
aiofiles==25.1.0
alembic==1.18.5
chex==0.1.92
colorlog==6.10.1
etils==1.14.0
-e git+https://github.com/trueagi-io/FabricPC.git@b6f64adf9314863ce665085a92d544807d585819#egg=fabricpc
flax==0.12.7
fsspec==2026.6.0
greenlet==3.5.3
humanize==4.16.0
jax==0.10.2
jaxlib==0.10.2
jaxtyping==0.3.11
Mako==1.3.12
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdurl==0.1.2
ml_dtypes==0.5.4
msgpack==1.2.1
numpy==2.5.1
opt_einsum==3.4.0
optax==0.2.8
optuna==4.9.0
orbax-checkpoint==0.12.1
packaging==26.2
prometheus_client==0.25.0
protobuf==7.35.1
psutil==7.2.2
Pygments==2.20.0
PyYAML==6.0.3
rich==15.0.0
scipy==1.18.0
simplejson==4.1.1
SQLAlchemy==2.0.51
tensorstore==0.1.84
toolz==1.1.0
tqdm==4.68.4
treescope==0.1.10
typing_extensions==4.16.0
uvloop==0.22.1
wadler_lindig==0.1.7
zipp==4.1.0
